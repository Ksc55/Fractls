export interface Asset {
    name: string;
    address: string;
    available: number;
}

export interface SelectedAsset {
    name: string;
    value: number;
}

export interface SocialLink {
    name: string;
    url: string;
}
